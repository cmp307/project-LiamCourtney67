<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ScottishGlenAssetTracking.Views.Employee.ViewEmployee"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScottishGlenAssetTracking.Views.Employee"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="#5C5C5C">
        <StackPanel>
            <TextBlock x:Name="ViewEmployeeHeader" Text="View Employee" FontSize="24" Margin="10"/>
            <ComboBox x:Name="DepartmentSelect" Header="Department" HorizontalAlignment="Stretch" PlaceholderText="Select Department" Margin="10" ItemsSource="{Binding}" DisplayMemberPath="Name" SelectionChanged="DepartmentSelect_SelectionChanged"/>
            <ComboBox x:Name="EmployeeSelect" Header="Employee" HorizontalAlignment="Stretch" PlaceholderText="Select Employee" Margin="10" ItemsSource="{Binding}" DisplayMemberPath="Name" SelectionChanged="EmployeeSelect_SelectionChanged"/>
            
            <StackPanel x:Name="ViewEmployeeView" Visibility="Collapsed">
                <TextBlock x:Name="EmployeeFirstName" Text="First Name: " Margin="10"/>
                <TextBlock x:Name="EmployeeLastName" Text="Last Name: " Margin="10"/>
                <TextBlock x:Name="EmployeeEmail" Text="Email: " Margin="10"/>
                <TextBlock x:Name="EmployeeDepartment" Text="Department: " Margin="10"/>
                <ListView x:Name="EmployeeAssets" Header="Assets: " ItemsSource="{Binding}" DisplayMemberPath="Name" Margin="10"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="EditEmployeeButton" Content="Edit Employee" Click="EditEmployeeButton_Click" Margin="10" Background="Blue" Foreground="White"/>
                    <Button x:Name="DeleteEmployeeButton" Content="Delete Employee" Click="DeleteEmployeeButton_Click" Margin="10" Background="Red" Foreground="White"/>
                </StackPanel>
                <TextBlock x:Name="Status" Margin="10" HorizontalAlignment="Center"/>
            </StackPanel>

            <StackPanel x:Name="EditEmployeeView" Visibility="Collapsed">
                <TextBox x:Name="EmployeeFirstNameInput" Header="First Name" Margin="10"/>
                <TextBox x:Name="EmployeeLastNameInput" Header="Last Name" Margin="10"/>
                <TextBox x:Name="EmployeeEmailInput" Header="Email" Margin="10"/>
                <ComboBox x:Name="EmployeeDepartmentSelect" Header="Department" HorizontalAlignment="Stretch" PlaceholderText="Select Department" Margin="10" ItemsSource="{Binding}" DisplayMemberPath="Name" SelectionChanged="EmployeeDepartmentSelect_SelectionChanged"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Margin="10" Background="Red" Foreground="White"/>
                    <Button x:Name="UpdateEmployeeButton" Content="Update Employee" Click="UpdateEmployeeButton_Click" Margin="10" Background="Green" Foreground="White"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
